import { FlatList } from 'react-native'
import styled from 'styled-components/native'

export const StyledCharactersList = styled(FlatList)`
  flex-direction: row;
  row-gap: 20px;
  justify-content: space-between;
`
